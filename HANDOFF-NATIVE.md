# Clear Week — 네이티브(React Native / Expo)로 넘기는 인수인계

이 문서는 **웹 프로토타입을 버리고 다시 짜는 사람**을 위한 것이다.
`index.html`의 구현은 넘어가지 않는다. 넘어가는 것은 **무엇을 만들지, 무엇을
하지 말지, 그리고 웹에서 밟은 함정들**이다.

같이 읽을 것:
- `clearweek-spec.md` — 결정과 그 이유. **이 문서와 충돌하면 spec이 맞다.**
- `CLAUDE.md` — 웹 구현 안내서. 네이티브에서는 대부분 무의미하지만
  "확인된 것"과 "알려진 미해결"은 유효하다.

---

## 0. 한 문장

**종이 주간 플래너를 그대로 옮긴 것.** 한 주가 한 화면에 고정되고, 손으로 긋고,
그은 것은 그 자리에 남는다.

---

## 1. 깨면 안 되는 규칙 (spec §2 · 설계 철학)

1. **취소선은 삭제가 아니라 축적이다.** 그은 항목은 그 자리에 남는다.
   흐려지지도, 접히지도, 아래로 내려가지도 않는다.
   **완료 항목을 숨기거나 정렬로 내리는 기능은 영구히 만들지 않는다.**
2. **자동 이월 없음.** 못 한 일은 다음 주로 넘어가지 않는다.
3. **알림·태그·통계·다크모드 없음.** 여기서 '통계'는 사용자에게 보여 주는
   달성률·연속기록·점수다. **앱 화면에 숫자가 나타나는 순간 위반이다.**
4. **애매하면 넣지 말고 물어본다.** 경쟁력은 기능 수가 아니라 덜어냄이다.
5. **손그림으로 남는 것은 취소선 하나뿐.** 칸을 나누는 세로선은 없다 —
   열은 여백으로만 갈린다.
6. **손이 덜 가는 쪽이 옳다.** 적은 것을 보면 알 수 있는 일은 사람에게 안 시킨다.
   (칸 나누기가 자동인 이유, 펜 판을 미리 깔아 둔 이유)

**주간 화면에는 머리말과 표뿐이다.** 만든 사람도, 백업도, 로그인도 전부
`내 계정` 화면 안에 있다. 푸터를 화면 아래 두려던 시도는 물렸다 — 한 주가
한 화면인 제품에서 아래 한 줄은 작지 않았다.

---

## 2. 화면

```
┌──────────────────────────────────────────────┐
│ 2026. 8.24-8.30  ‹ ›        🔊  👤          │  머리말
├───────────────────────────────┬──────────────┤
│ 24.M  항목  항목              │ note         │
│ 회식  항목                     │ 항목         │  ← 요일 아래 메모(6.5pt)
├───────────────────────────────┤ 항목         │
│ 25.T  항목                     │              │
│ …7칸…                          │              │
└───────────────────────────────┴──────────────┘
```

- 왼쪽 7칸(월~일) 세로로 균등 + 오른쪽 `note` 한 칸(날짜 없는 것들).
- **스크롤 없음.** 한 주가 한 화면에 고정.
- 칸이 넘치면 **저절로 2단·3단으로 나뉜다** (최대 3단). 개수가 아니라
  **실제로 넘치는지**를 본다 — 화면 높이에 따라 한 단에 3줄이냐 4줄이냐가 다르다.
- 나뉘면 그 칸의 글씨가 작아진다 (`--split-scale-1/2`).
- 그래도 넘치면 칸 오른쪽 아래에 `+3`. **숨기거나 접지 않고, 잘렸다는 사실만 알린다.**
- 주 이동은 **날짜 옆** 화살표. 지난 주도 편집 가능.
- `today`는 이번 주를 벗어났을 때만 화살표 오른쪽에 나타난다.

---

## 3. 제스처 — 이 제품의 전부

| 제스처 | 동작 |
|---|---|
| 요일 칸 **빈 곳** · **글자 옆 빈 자리** 탭 | 그 칸에 항목 추가 (＋ 버튼 없음) |
| **왼쪽 여백**(날짜와 그 아래) 탭 | 요일 아래 메모 (6.5pt 세미볼드) |
| 항목 탭 | 편집 (**그어진 항목은 불가**) |
| 항목 위 가로 드래그 | 긋기 — 지나간 만큼만, 줄 단위 |
| **왼쪽 여백에서 시작해 가로로** | 그 높이의 항목을 긋는다 |
| 그어진 줄 위 좌우 문대기 | 지우개 — 문지른 자리만 잉크가 벗겨짐 |
| 넓게 문대기 | 지나간 줄이 전부 지워진다 (칸·단을 넘어가도) |
| 항목 더블탭 | 남은 줄까지 자동으로 긋기 |
| 항목 롱프레스 0.5초 | 그 항목 삭제 |
| 롱프레스 후 그대로 드래그 | 지나간 항목 전부 삭제 |
| **펜을 빈 곳에 대고 그냥 쓰기** | 크게 적힌다 — **탭 없음** |
| 아무 데서나 아래로 당기기 | 새로고침 |
| 머리말 스피커 탭 | 소리 켜고 끄기 |
| 머리말 사람 탭 · 제목 롱프레스 | 내 계정 |

**요일 아래 메모도 항목과 똑같이** 긋고·지우고·더블탭·탭 편집·롱프레스 삭제된다.

### 제스처 공간이 겹치지 않게 설계돼 있다

새 제스처를 넣기 전에 이 조합을 먼저 확인할 것:
**(가로 / 세로) × (그어짐 / 안 그어짐) × (항목 / 빈 곳 / 여백)**

- **가로**는 전부 긋기·지우기의 것이다.
- **세로**는 당겨서 새로고침 하나뿐. 항목 위에서 시작한 세로는 아무것도 아니다.
- 칸 안 세로 드래그는 아직 비어 있다.

---

## 4. 웹에서 밟은 함정 — 네이티브에서도 형태만 바뀌어 나타날 것들

### 4-1. 애플펜슬 손글씨 변환(스크리블)은 포인터를 안 따른다 ★가장 크게 밟음

**증상**: 이미 적힌 글자 위에 펜으로 선을 그으면 취소선이 아니라 **새 글자로
인식됐다.**

**원인**: 스크리블은 포인터 히트 테스트를 따르지 않는다. iOS가 그 언저리의
텍스트 입력창을 찾아 획을 가져간다. 입력창이 글자 **밑에** 깔려 있기만 해도
그 위의 펜을 낚아챈다. z-index로 못 막는다.

**해법**: **글자가 있는 높이에는 입력창을 두지 않는다.** 웹에서는 칸마다 투명
입력창을 깔되 "적힌 것들 중 가장 아래" 밑에서 시작하게 했다.

**RN에서**: `TextInput`은 진짜 `UITextField`/`UITextView`라 스크리블이 그대로
돈다. 같은 함정도 그대로 온다. **긋기 영역과 `TextInput` 영역이 겹치면 안 된다.**

또 하나: **스크리블은 포인터 이벤트 없이 포커스만 준다.** 그래서 "펜이냐 손가락이냐"를
`pointerType`으로 가르면 안 된다. 웹에서는 **포커스가 온 시각**으로 갈랐다
(손가락이 누른 직후 400ms 안의 포커스는 손가락 것).

### 4-2. 입력창을 목록 안에 끼우면 적힌 것이 밀려난다

`overflow: hidden`인 상자도 **포커스를 주면 브라우저가 굴린다.** 칸에 적힌
것들이 밀려 사라졌다. RN에서는 키보드 회피 동작이 같은 일을 한다 —
`ScrollView`의 자동 스크롤, `KeyboardAvoidingView`. 한 화면 고정 레이아웃에서는
**입력창을 목록에 끼우지 말고 얹는 것**이 안전하다.

### 4-3. 무음 스위치가 Web Audio를 통째로 막는다

iOS·iPadOS는 무음이면 Web Audio를 막는다(미디어 요소는 안 막힘). **왜 안 나는지
알 길이 없다** — 아이패드는 물리 스위치도 없다.

**해법**: 기기 무음을 **무시**하고, 대신 **끌 손잡이를 머리말에 내놓았다**(스피커).
**무시할 권리는 끌 자리를 준 다음에 생긴다.**

**RN에서**: `expo-audio`의 `playsInSilentMode`. 같은 판단을 그대로 가져갈 것.

### 4-4. 두 소리의 음량은 같은 눈금이 아니다

사각사각(이어지는 소리)과 타자 톡(26ms 한 순간)은 **적힌 값이 같아도 귀에
다르게 들린다** — 귀는 200ms쯤을 한 덩어리로 듣는다. 파형으로 −4.5 dB를
맞춘 것이 실기기에서도 맞았다. 그래서 `keyVolume`이 1을 넘는다.

### 4-5. 소리를 끌 때는 재생 갈래도 놓아 줘야 한다

끄고도 오디오 세션을 붙들고 있으면 **소리는 안 나는데 남의 앱 소리를
계속 가로챈다.** 끌 때 세션을 `auto`로 놓아 준다.

### 4-6. 취소선을 좌표로 저장하면 안 된다

**시드 + 줄 안에서의 시작·끝 비율**로 저장한다. 용량이 작고, 화면 폭이
바뀌어도 따라간다. 같은 시드는 같은 획을 만든다.

```
strikes: [{ line: 0, a: 0.01, b: 0.97, seed: 1074304443 }]
```

**부분적으로 지워진 상태는 저장하지 않는다.** 손을 뗄 때 둘 중 하나로 정리:
70% 이상 지워졌으면 마저 지우고, 아니면 잉크를 온전히 되돌린다.
(양 끝은 문대는 손이 되돌아가는 자리라 한 번밖에 안 지나가서 늘 잔흔이 남았다)

### 4-7. 지우개는 손짓을 시작한 항목에 갇히면 안 된다

포인터 캡처 때문에 넓게 문대도 한 줄씩만 지워졌다. **매번 그 자리의 항목을
찾아** 문대야 칸도 단도 넘어간다. 손을 뗄 때의 정리도 **닿았던 항목 전부**에
걸어야 한다 — 주인만 정리하면 남의 항목에 벗겨진 잉크가 남는다.

### 4-8. 적은 것을 지키는 장치는 `blur` 말고도 둘 더 있다

모바일 자판의 blur는 언제 오는지 믿을 수 없고, 앱이 뒤로 넘어가면 아예 안 온다.
**딴 데를 누르는 순간**과 **화면을 떠나는 순간**에도 확정한다.
RN에서는 `AppState` 변화가 그 자리다.

---

## 5. 데이터 모델 (그대로 가져갈 것)

```js
{
  weekId: "2026-W35",                       // ISO 주. 월요일 기준
  days: {
    mon: [{ id, text, struck, createdAt, updatedAt, strikes: [] }],
    tue: [...], …, sun: [...],
    free: [...]                             // note 칸 — 날짜 없음
  },
  notes:       { mon: "8/20 마감", … },      // 요일 아래 메모
  noteAt:      { mon: 1755000000000, … },   // 메모의 시각
  noteStrikes: { mon: [{line,a,b,seed}], …},// 메모에 그은 획
  graves:      { "<id>": 1755000000000 }    // 지운 표시 (60일 보관)
}
```

**규칙 넷**

1. **`graves`가 없으면 한쪽에서 지운 것이 다른 쪽에서 되살아난다.**
2. **메모는 글자와 획이 한 몸이다.** `noteAt` 하나로 이긴 쪽을 통째로 가져온다.
   따로 합치면 이쪽 글자에 저쪽 획이 얹혀 엉뚱한 줄에 줄이 간다.
3. **열 수는 저장하지 않는다.** 매번 계산한다.
4. **합치기 규칙은 한 곳에만 둔다.** 두 곳에 두면 갈라지고, 갈라지면 무엇이
   맞는지 아무도 모른다. (웹에서는 `functions/_lib.js`)

**합치기**: 같은 항목은 나중에 고친 쪽(`updatedAt`). 지운 표시보다 나중에
고쳤으면 고친 쪽이 이긴다 — **지워지는 것보다 남는 것이 낫다.**
순서는 `createdAt`으로 (배열 순서를 쓰면 기기마다 달라진다).
시각은 1e12를 넘으므로 `|0`을 쓰면 안 된다.

---

## 6. 튜닝 값 — 손과 귀로 맞춘 것들

바꿔도 되지만, **이 값들은 실제로 만져 보고 정해진 것**이다. 특히 ★는 실기기에서 확인됨.

### 크기

| 이름 | 값 | 비고 |
|---|---|---|
| 항목 글자 | 9.5pt | 종이 플래너의 작은 손글씨 |
| 요일 아래 메모 | 6.5pt 세미볼드 | |
| 펜으로 쓸 때 | 26px | 이만큼은 돼야 팔이 펴진다 |
| 항목 높이 | 26px | **미검증** — 초안은 "긋기를 위해 44px 양보 불가"였다 |
| 날짜 라벨 폭 | 38px | 들여쓰기 기준 |
| 열 간격 | 16px | 세로선이 없으므로 여백이 경계 |
| 나눔 배율 | 0.84 / 0.72 | 1단 / 2단 나눴을 때 |

### 색

```
paper      #ffffff
ink        #1d2b50   본문
ink-faint  #9a9eab   보조
rule       #dedede   칸 구분선
잉크(획)    #26356b
```

**색은 안내 카드에서만 쓴다** (주황·초록·빨강·파랑). 주간 표는 단색.

### 획

```
noiseAmplitude 0.4    흔들림 진폭(px). 0이면 완전 직선
noiseFrequency 0.012
widthMin 1.6 / widthMax 3.2    빠르게=얇게, 천천히=굵게
speedRef 1.4          이 속도(px/ms)에서 widthMin
tiltMax 0.015         획 전체의 무작위 기울기
followY 0.15          손가락 상하 움직임 반영 비율
commitRatio 0.6       줄 폭 대비 이만큼 지나가야 확정
slopPx 8              긋기 방향 판정
doubleTapMs 260
longPressMs 500
overshoot 2           자동 획이 글자 밖으로 삐져나가는 양
autoStrokeMs 220 / autoStagger 70
rewindMs 200          미달 시 되감기
```

### 지우개

```
radius 16             지우개 반경
reachPx 14            줄 위아래 이만큼까지 그 줄로 친다
passStrength 0.55     한 번 지나갈 때 지워지는 정도
doneThreshold 0.85
finishRatio 0.7       손 뗄 때 이 이상이면 마저 지운다
bucketPx 8
```

### 소리 ★

```
사각사각  volume 0.20  bandHz 1900  bandQ 0.9  floor 0.25
         attackMs 10  releaseMs 80  speedRef 1.0
타자 톡   keyVolume 1.4  keyHz 2400  keyQ 1.2  keyMs 26
```

★ 이 균형은 실기기에서 확인됐다 (2026-08-21). **`keyVolume`이 1을 넘는 것은
오타가 아니다** — 4-4 참고.

### 그 밖

```
maxColumns 3          한 칸 최대 단 수
PULL.distance 72      이만큼 당기면 새로고침
PAD.guardMs 400       손가락 포커스를 거르는 시간
PAD.minRoom 22        이보다 좁으면 펜 판을 안 둔다
```

---

## 7. 첫 실행 안내 (spec §4-4)

버튼이 하나도 없는 화면이라 첫 단서는 필요하다. **카드 한 장, 줄 넷.**
줄마다 표시 하나와 한 문장:

```
＋  빈 곳을 눌러 생성 (요일 아래, 요일 칸, 노트)
↔  글자 위를 그어서 완료. 문지르면 지워져요
✕  꾹 누르면 삭제
👤 로그인하면 기기간 연동할 수 있습니다
```

- **한 번에 한 줄만 진하다.** 아무 데나 누르면 다음 줄로 내려가고,
  **끝 줄에 닿아야 닫는 손잡이가 살아난다.**
- `다시 보지 않기`는 **체크상자**다. 체크하고 닫아야 영영 안 뜬다.
- **안내는 앱의 상태를 건드리지 않는다.** 표본 주를 그리고 셀로판지를 덧대던
  방식은 물렸다 — 저장·동기화 경로에 "지금은 표본이니 멈춰라" 가드를 심어야 했다.

---

## 8. 로그인 · 동기화 · 캘린더

**웹의 현재 상태** (네이티브에서 다시 판단할 것):

- **Firebase Auth 하나로 통일**돼 있다. 이메일+비밀번호, 없으면 자동 생성.
  프로젝트: `dada-calendar-524ec` (캘린더X와 같은 계정).
- 주 데이터는 Cloudflare KV (`week:<email>:<weekId>`), 요청마다 Firebase
  ID token 검증.
- **캘린더X에서 받아만 온다** (한 방향). Clear Week에서 적은 것도 지운 것도
  저쪽으로 안 간다. 일정의 원본은 캘린더에 있고, 주간표는 그것을 종이 위에
  펼쳐 보는 자리다 — **종이에 그은 것이 원본을 바꾸면 안 된다.**
  - 받아 오는 것: `kind === 'task'` 중 안 끝난 것, 반복 아닌 것, 이 주에 걸린 것
  - 저쪽에서 사라지면 이쪽에서도 지운다
  - 저쪽 id에서 이쪽 id를 유도 (`dc-<origId>`) → 몇 번 받아도 중복 없음

**네이티브에서 다시 볼 것**: spec §11은 "**로그인은 검증 도구다. 최종 형태인
네이티브에서는 iCloud가 계정 없이 맞춰 주므로 통째로 사라져야 한다**"고 적어
두었다. CloudKit으로 갈아탈지, Firebase를 유지할지가 갈림길이다.
캘린더X 연동을 유지하려면 Firebase가 편하다.

---

## 9. 실기기에서 확인된 것 (사람이 봐 준 것만)

- **연달아 긋기가 남는 것** (2026-08-21)
- **사각사각과 타자 톡의 음량 균형** (2026-08-21)
- **아이패드에서 소리가 나는 것** (2026-08-21)
- **애플펜슬 손글씨 변환이 실제로 도는 것** (2026-08-24) — 빈 곳에 펜을 대고
  그냥 쓰면 탭 없이 글자로 들어간다
- **홈 화면 앱 상단 바 흰색** (2026-08-24)

**"확인했습니다"라고 쓰지 말 것.** 사람이 실기기에서 봐 준 것만 이 목록에 온다.

---

## 10. 아직 모르는 것 / 미해결

1. **되돌리기 없음.** 롱프레스 삭제는 확인도 취소도 없다.
2. **항목 높이 26px는 검증되지 않았다.** 초안은 "긋기를 위해 44px 양보 불가"였는데
   한 화면 고정 요구에 밀려 내려갔다. 손맛에 영향이 있는지 미확인.
3. **동시에 밀면 한쪽이 덮일 수 있다** (KV에 조건부 쓰기 없음). 다음 차례에
   스스로 회복되지만 근본 해결은 Durable Objects.
4. **캘린더에서 날짜만 옮긴 것**은 "지운 것"으로 보인다. 옮기기는 지원 안 함.
5. **백업 경로가 실기기 미검증** — 아이폰 공유 시트로 파일이 제대로 떨어지는지.

---

## 11. React Native / Expo로 옮길 때

### 무엇으로 그리나 — 여기가 제일 중요하다

취소선은 **점마다 굵기가 다른 손그림**이다 (속도에 따라 1.6~3.2px, 미세한 흔들림,
무작위 기울기). 그리고 **부분 지우기**가 필요하다 (문지른 자리만 잉크가 벗겨짐 —
웹에서는 SVG mask로 했다).

- **`@shopify/react-native-skia`** — 이 요구에 맞는 유일한 현실적 선택.
  가변 굵기 path, 마스크/블렌드모드, 60fps 손그림 다 된다.
- `react-native-svg`는 마스크가 되지만 매 프레임 path를 다시 만드는 비용이 크다.

제스처는 **`react-native-gesture-handler`** + **`react-native-reanimated`**.
긋는 동안 JS 브리지를 타면 안 된다 — worklet에서 처리할 것.

### 매핑

| 웹 | RN / Expo |
|---|---|
| SVG path + mask | Skia `Path` + `Mask` / `BlendMode` |
| Pointer Events | `react-native-gesture-handler` (Pan/LongPress/Tap) |
| Web Audio | `expo-audio` (`playsInSilentMode` 필수 — 4-3) |
| `navigator.vibrate` (iOS에서 무시됨) | **`expo-haptics` — 실제로 된다.** 웹에서 못 하던 것 |
| `localStorage` | `expo-sqlite` 또는 `react-native-mmkv` |
| 투명 `<input>` 깔기 | `TextInput` (스크리블 그대로 됨, 4-1 함정도 그대로) |
| `env(safe-area-inset-*)` | `react-native-safe-area-context` |
| `fetch` (Firebase REST) | 그대로 쓰거나 `@react-native-firebase` |
| 당겨서 새로고침 | 직접 구현 (스크롤이 없으므로 `RefreshControl` 못 씀) |

### 위젯 — 된다

**§10의 "Swift Playgrounds로는 위젯 못 만든다"는 Expo에서는 해당 없다.**
`@bacons/apple-targets` 설정 플러그인 + EAS Build로 WidgetKit 위젯을 만들 수 있다.

**단, 위젯은 Swift다** — JS/React Native가 안 돈다. 앱과 위젯은 **App Group
공유 컨테이너**로만 통신한다.

→ **데이터 저장을 처음부터 App Group 컨테이너에** 두는 것이 좋다.
   위젯이 읽을 수 있는 형태(공유 `UserDefaults` 또는 컨테이너 안 JSON 파일)로.
   나중에 옮기려면 마이그레이션이 생긴다.

**위젯이 실질적인 제품이다** — "한 주가 항상 펼쳐져 있음"이 이 제품의 경쟁력이고,
손맛은 계속 쓰게 만드는 장치다.

### 실기기 설치

EAS Build → **internal distribution** 프로파일이면 Mac 없이 실기기에 설치된다.
기기 UDID를 프로비저닝에 등록해야 한다 (`eas device:create`).
개발자 계정 $99/년은 필요하다.

### 만드는 순서 제안

1. **긋기 하나만.** 더미 항목 5개 + Skia로 손그림 취소선. 실기기에서 손맛 확인.
   → **여기서 웹보다 나은지가 갈린다. 아니면 그 뒤는 의미 없다.**
2. 지우개 (부분 지우기 + 손 뗄 때 정리)
3. 7+1 주간 화면 + 저장 + 주 이동 + 칸 자동 나누기
4. 펜 입력 (4-1 함정 조심)
5. 소리 · 햅틱
6. **위젯** — App Group 데이터 공유
7. 동기화 · 캘린더 연동 (CloudKit이냐 Firebase냐 결정)

---

## 12. 웹 버전은 어디에

- 저장소: `stupidpoohh-tech/clear-week`
- 브랜치: `claude/clear-week-planner-y2glcy` (기본 브랜치, Cloudflare Pages 배포 대상)
- 배포: https://clear-week.pages.dev
- 검사: `node tests/smoke.mjs` (307) · `tests/merge.mjs` (21) · `tests/log.mjs` (49)

**네이티브가 자리 잡을 때까지 웹은 지우지 말 것.** 손맛 비교의 기준선이다.
