# Clear Week — 1단계 웹 프로토타입 기획안

> 이 문서는 Claude Code가 읽고 바로 구현에 착수하기 위한 명세입니다.
> **범위는 1단계 프로토타입 한정입니다.** 아래 "이번에 만들지 않는 것"을 반드시 지킬 것.

---

## 0. 이 프로토타입의 목적

단 하나의 가설을 검증합니다.

> **손가락으로 글자 위를 드래그해서 선을 긋는 감각이, 종이에 펜으로 긋는 감각에 근접하는가.**

이 앱의 최종 형태는 iOS 네이티브 + 홈화면 위젯입니다. 그러나 위젯을 만들기 전에
"긋는 손맛"이 성립하지 않으면 제품 자체가 성립하지 않습니다.
따라서 이 단계는 **제스처 감각 테스트 도구**이지, 출시용 제품이 아닙니다.

**검증 방법**: 기획자 본인이 아이폰 사파리에서 홈 화면에 추가하여 2주간 실사용.

---

## 1. 제품 개요

종이 주간 플래너를 그대로 옮긴 앱. 동사는 셋뿐입니다.

| 동사 | 의미 | 1단계 구현 |
|---|---|---|
| 적기 | 칸에 할 일을 쓴다 | O |
| 긋기 | 다 한 것에 취소선을 긋는다 | O ← **핵심** |
| 보여주기 | 홈화면에 한 주가 항상 펼쳐져 있다 | X (네이티브 위젯 필요, 2단계) |

### 설계 철학 — 구현 중 판단이 필요할 때 여기로 돌아올 것

1. **취소선은 삭제가 아니라 축적이다.**
   그은 항목은 사라지지 않고 그 자리에 남는다. 흐려지지도, 접히지도, 맨 아래로
   내려가지도 않는다. 한 주가 끝날 때 화면이 취소선으로 가득 차 있는 것이 목표 상태다.
2. **자동 이월 없음.**
   못 한 일은 다음 주로 넘어가지 않는다. 다시 적는 행위 자체가 재약속이다.
3. **연동 없음, 계정 없음, 동기화 없음.**
   캘린더·알림·태그·우선순위·반복·소셜 전부 없음.
4. **기능 추가 요청이 애매하면 넣지 않는다.**
   이 제품의 경쟁력은 기능 수가 아니라 덜어냄이다.

---

## 2. 이번에 만들지 않는 것 (엄수)

- ~~로그인 / 계정 / 서버 / 클라우드 동기화~~ → **뒤집었다 (2026-08-20). 아래 §11.**
- 알림 / 리마인더
- 카테고리 / 태그 / 색상 라벨 / 우선순위
- 반복 일정
- 캘린더 앱 연동
- 통계 / 달성률 / 연속 기록 / 점수
- 드로잉·낙서 레이어
- 다크모드 (1단계 제외, 2단계에서 검토)
- ~~온보딩 튜토리얼~~ → **뒤집었다 (2026-08-20). 아래 §4-4.**
- ~~설정 화면~~ → **뒤집었다 (2026-08-20). 아래 §12.** 로그인이 생기면 계정을 두는 자리가 있어야 한다.

**완료 항목을 숨기거나 정렬로 내리는 기능은 이 제품에서 영구히 만들지 않습니다.**

---

## 3. 화면 구조

화면은 하나입니다. 라우팅 없음.

```
┌───────────────────────────┬──────┐
│  2026. 8.17-8.23     ‹  ›  │      │  ← 연도 + 그 주의 월~일 날짜 범위
├───────────────────────────┤ note │  ← 8번째 칸의 라벨. 누르면 입력
│ 17.M ─회의자료 정리─       │      │
│      장보기                │ ─영어─│    요일 없는 자유 칸.
├───────────────────────────┤  단어 │    오른쪽 세로 1열,
│ 20.T 멜팅의원 │ J38        │      │    종이 플래너의 우측 여백
│ 마감 웨비나   │ G44        │ 책반납│    (마감 = 라벨 아래 메모)
│      낭만백수달│ 회의준비   │      │
├───────────────────────────┤      │
│ ... FRI SAT SUN ...       │      │
└───────────────────────────┴──────┘
        ↑ 라벨 옆에 바로 첫 항목        ↑ 세로 구분선은 전체 높이를 관통
                 ↑ 칸 안에 세로선을 그으면 열이 하나 늘어난다
```

### 7+1 구조

- 월~일 7칸 + 요일에 묶이지 않는 자유 칸 1개 = 총 8칸.
- 8번째 칸은 종이 플래너 우측 여백에 해당. 라벨은 `note`.
- 8번째 칸의 항목은 주가 바뀌어도 유지할지 여부: **1단계에서는 주별로 분리** (단순하게).

### 배치 — 왼쪽 7칸 세로 스크롤 + 오른쪽 자유 칸 1열

8칸을 전부 가로로 나누면 칸이 너무 좁아 손가락 드래그가 어렵습니다. 대신 **2열**로 둡니다.

- **왼쪽**: 월~일 7칸이 위에서 아래로 쌓인다. 칸마다 아래쪽 구분선.
- **오른쪽**: 자유 칸 하나가 길고 얇은 세로 열로 전체 높이를 관통한다.
  요일 구분선은 이 열을 침범하지 않고, 세로 구분선 하나가 위아래를 갈라놓는다.
- 폭은 `--free-width`(기본 `clamp(84px, 27%, 132px)`), 글자 크기는 `--free-font`(9.5pt).
  좁은 칸이라 항목이 두세 줄로 감기며, 줄마다 따로 긋거나 더블탭으로 한 번에 긋는다.

### 한 주 = 한 장. 스크롤 없음

종이 플래너의 한 장처럼, **한 주가 화면 하나에 통째로 들어가고 스크롤되지 않는다.**
왼쪽 7칸은 화면 높이를 7등분해 나눠 갖고, 오른쪽 note 칸이 전체 높이를 관통한다.

- 요일 라벨은 왼쪽 여백(`--label-w` 42px)에 놓이고 **첫 항목이 라벨 바로 옆에서 시작**한다.
  둘째 줄부터는 라벨 폭만큼 들여써져 첫 항목과 세로로 맞는다.
- 칸 높이가 정해져 있으므로 넘치는 항목은 잘린다. 잘렸다면 아래 "칸 나누기"로 자리를 만든다.
- **잘렸으면 잘렸다고 말한다.** 칸 밖으로 밀린 항목 수가 그 칸 오른쪽 아래에
  `+3`으로 아주 작게(6.5pt, 보조색) 붙는다. 데이터에는 있는데 화면에 없는 상태를
  사용자가 모르면 그건 조용한 데이터 손실이다. 표시는 사실만 알리고 아무것도 하지 않는다
  — 항목을 숨기거나 접거나 스크롤을 만들지 않는다. 칸을 나누면 자리가 생겨 저절로 줄어든다.
  (2026-08-19: 미해결 1번 해소)

### 칸 나누기 — 칸이 스스로 나뉜다 (2026-08-19 개정)

**초안은 세로선을 손으로 긋게 했다. 걷어냈다.** 이유는 둘이다.
칸마다 선이 그이면 표가 선으로 지저분해지고, 무엇보다 **적을 때마다 손이 한 번 더 간다.**
얼마나 나눌지는 적은 항목 수를 보면 이미 알 수 있는데, 그걸 사람에게 시킬 이유가 없다.

- **한 열에 안 들어갈 때만 나뉜다.** 항목 수로 못 박지 않는다 (2026-08-20 개정).
  한 열에 들어가는 줄 수는 화면 높이가 정한다 — 실측하면 iPhone SE급 3줄,
  390×760 3줄, 430×932 4줄이다. 숫자를 박으면 큰 폰에서는 자리가 남는데 나뉘고
  작은 폰에서는 나뉘어도 잘린다. 그래서 한 열로 그려 보고, 넘치면 하나 늘려
  다시 재 본다 (`AUTO_SPLIT.maxColumns` 3). 지우면 다시 합쳐진다.
  **덕분에 `+N`(잘림 표시)은 3단으로도 모자랄 때만 나타난다.**
- **세로선은 그리지 않는다.** 열은 여백(`--col-gap` 16px)으로만 갈린다.
  화면에 남는 선은 요일 구분선과 note 칸 경계뿐이고,
  손그림으로 남는 것은 여전히 취소선 하나뿐이다.
- **고르게 나눠 담는다**(`column-fill: balance`). 왼쪽부터 채우면 7개일 때
  셋째 단이 비어서, 글씨만 작아지고 자리는 안 생긴다.
  나눈 만큼 실제로 나눠 담겨야 나눈 값을 한다.
- **나눌수록 그 칸의 글씨와 행이 작아진다** — 좁아진 칸에 작게 쓰는 종이의 방식.
  기본 → `--split-scale-1`(0.84) → `--split-scale-2`(0.72). 나눈 칸에만 적용된다.
- **note 칸은 나뉘지 않는다.** 화면 높이 전체를 쓰는 열이라 한 열에 스무 개 넘게 들어간다.
  3개에 나누면 폭 40px짜리 열이 된다.
- 열 수는 저장하지 않는다. 항목에서 매번 다시 계산된다 — 데이터에 `splits`가 없어졌다.

**세로 드래그 자리가 다시 비었다.** 페이지 스크롤이 없고 칸 나누기도 손을 뗐으므로,
칸 안 세로 드래그는 지금 아무 동작에도 묶여 있지 않다. 새 제스처를 넣는다면 여기다.

### 주 이동

- 상단에 연도와 날짜 범위(`2026. 8.17-8.23`), 칸 라벨에 날짜와 요일 머리글자(`20.T`).
  연도는 맨 앞에 적히는 월요일 기준 — 해가 바뀌는 주는 `2025. 12.29-1.4`처럼 나온다.
  요일 머리글자는 달력 관례대로 `M T W T F S S` (화·목, 토·일이 같은 글자지만 날짜가 붙어 있어 헷갈리지 않는다).
- 상단 화살표로 이전/다음 주. (좌우 스와이프는 긋기·지우기와 충돌하므로 제외)
- **`today`는 이번 주를 벗어났을 때만 화살표 왼쪽에 나타난다** (2026-08-20).
  넘겨 놓고 돌아오려면 화살표를 여러 번 눌러야 했다. 이번 주를 보고 있을 때는
  할 일이 없으므로 나타나지 않는다 — 평소 머리말은 예전과 같다.
- **지난 주도 고쳐 쓸 수 있다** (2026-08-20 개정). 초안은 "넘긴 페이지는 고쳐 쓰지
  않는다"며 편집을 막았지만, 종이 플래너는 지난 장을 막지 않는다. 어제 못 그은
  취소선을 오늘 긋는 것을 앱이 거절할 이유가 없다. 자동 이월이 없는 것과 별개다 —
  **이월을 안 하는 것은 규칙이고, 지난 장을 잠그는 것은 참견이었다.**
- 다음 주는 미리 적을 수 있음.

---

## 4. 인터랙션 명세 — 여기가 이 프로젝트의 전부

### 4-1. 적기

- **칸의 빈 곳이나 날짜 라벨(`17.M`… 과 자유 칸의 `note`)을 살짝 탭하면** 그 칸 목록
  맨 아래에 인라인 입력창이 열린다. 모달·시트 금지. `＋` 버튼은 두지 않는다.
  **빈 곳까지 과녁으로 삼은 것은 2026-08-20** — 라벨만으로는 38×18px이라 너무 작았다.
  항목·라벨 위에는 각자의 제스처가 있으므로 건드리지 않고, 손이 10px 이상 움직였으면
  (긋기·훑어 지우기) 열지 않는다.
- **입력창은 `pointerup`이 아니라 `click`에서 연다.** 터치 브라우저는 click 시점에
  버튼에 포커스를 주기 때문에, pointerup에서 열면 방금 `focus()`한 입력창이 그 click에
  포커스를 빼앗겨 곧바로 닫힌다. **안드로이드에서 항목 생성이 아예 안 되던 원인이 이것이다**
  (2026-08-20). 데스크톱은 mousedown에서 포커스가 가므로 드러나지 않았다.
- **라벨을 꾹 누른 채 아래로 끌면** 요일 아래에 작은 메모를 적을 수 있다.
  6.5pt 세미볼드(`--sublabel-font`). 날짜·마감처럼 그 요일 전체에 붙는 한 마디용.
  적어 둔 메모를 탭하면 다시 고칠 수 있고, 비우면 사라진다.
- 키보드 엔터 = 저장 후 **연속 입력 유지** (다음 줄 입력창이 바로 이어짐).
- **적다가 엔터를 치지 않고 딴 데를 눌러도 저장된다.** 엔터와 같은 취급.
  **`blur` 하나만 믿지 않는다** (2026-08-20) — 모바일 자판은 blur가 오는 시점이
  제각각이고, 앱이 뒤로 넘어가면 아예 안 오기도 한다. 그래서 세 곳에서 확정한다:
  `blur` · **딴 데를 누르는 순간**(document 캡처 단계 — 다른 손잡이보다 먼저) ·
  **화면을 떠나는 순간**(`pagehide` / `visibilitychange`).
  확정은 여러 번 불려도 한 번만 먹는다. 적은 것이 사라지는 것보다 나쁜 일은 없다.
- 빈 입력 상태에서 엔터 또는 포커스 아웃 = 입력 종료.
- 항목 최대 길이 제한 없음. 길면 두 줄로 감싸짐(wrap).
- 기존 항목을 탭하면 편집. **단, 이미 그어진 항목은 편집 불가.**

### 4-2. 긋기 ← 최우선 구현 대상

**동작**: 항목 텍스트 위를 손가락으로 좌→우 (또는 우→좌) 드래그하면, 손가락을 따라
선이 실시간으로 그려진다. 텍스트 폭의 일정 비율 이상 지나가면 완료로 확정된다.

**필수 요구사항**

| 항목 | 명세 |
|---|---|
| 실시간 추적 | 손가락 위치를 따라 선이 **자라난다**. 떼는 순간 나타나는 방식 금지 |
| 확정 임계값 | 텍스트 폭의 약 60% 이상 통과 시 확정. 미달 시 선이 되감기며 사라짐 |
| 선의 형태 | 완전한 직선 금지. 미세하게 흔들리는 손그림 획 |
| 획 끝 | 시작·끝이 살짝 삐져나감. 붓/펜의 관성 표현 |
| 굵기 변화 | 중간이 굵고 양끝이 얇거나, 속도에 따라 굵기 변화 |
| 확정 피드백 | 햅틱 (`navigator.vibrate` 또는 iOS 대체 수단) + 아주 짧은 확정 애니메이션 |
| 되돌리기 | 그어진 항목을 **길게 누르면** 선이 되감기며 사라짐 (0.5초 롱프레스) |
| 스크롤 충돌 | 가로 드래그는 긋기, 세로 드래그는 스크롤. 초기 이동 방향으로 판정 |

**구현 방향 제안**

- 각 항목 위에 SVG 오버레이를 두고, 드래그 좌표를 받아 `<path>`의 `d`를 실시간 갱신.
- 손맛의 핵심은 **경로에 미세한 노이즈를 주는 것**과 **가속도 반영**.
- 여러 변형을 만들어 비교할 수 있게, 획 파라미터(노이즈 진폭, 굵기 곡선, 임계값)를
  코드 상단 상수 객체로 분리할 것. 실사용 중 튜닝 예정.

```js
// 예시 — 이 형태로 상단에 노출
const STROKE = {
  noiseAmplitude: 1.8,   // 획 흔들림 (px)
  noiseFrequency: 0.08,
  widthMin: 1.6,
  widthMax: 3.2,
  overshoot: 6,          // 양끝 삐져나감 (px)
  commitRatio: 0.6,      // 확정 임계값
  hapticOnCommit: true,
};
```

**대안 제스처도 함께 구현**: 항목을 **탭 두 번(더블탭)** 하면 왼쪽에서 오른쪽으로
선이 자동으로 그어지는 애니메이션. 드래그가 번거로운 상황을 위한 보조 수단이며,
어느 쪽이 실사용에서 손이 가는지 비교하기 위함.

### 4-4. 첫 실행 안내 — 초안의 "온보딩 튜토리얼 없음"을 뒤집는다 (2026-08-20)

초안이 온보딩을 금지한 이유는 옳다. 설명이 필요한 인터페이스는 대개 잘못 만든 것이다.
그런데 이 앱은 **버튼이 하나도 없다.** ＋도, 메뉴도, 설정도 없앤 결과
제스처가 아홉 개까지 늘었는데, 화면에는 단서가 하나도 없다.
**"날짜 라벨을 누르면 적힌다"를 알아낼 방법이 없으면 첫 화면에서 끝난다.**
금지 조항을 쓸 때는 제스처가 둘뿐이었다. 조항이 아니라 상황이 달라졌다.

그래도 온보딩은 온보딩이므로, 가장 작은 형태로 둔다.

- **한 장뿐. 그리고 가리지 않는다** (2026-08-20 개정).
  베일을 덮고 가운데에 목록을 띄우는 방식을 걷어냈다. 대신 **표본 주를 실제로 그리고,
  그 옆에 한 마디씩 붙인다.** 적힌 항목, 그어진 취소선이 실물로 보이고
  설명은 그것을 가리킨다. 읽고 나서 옮겨 생각할 것이 없다.
- **한 칸에 한 가지씩.** 월=적기(라벨에 동그라미), 화=긋기, 수=문대서 지우기,
  목=꾹 눌러 삭제, 금=그은 것은 남는다·칸은 저절로 나뉜다.
  한 칸에 두 마디를 붙이면 설명끼리 겹친다.
- **표본은 저장하지 않는다.** 여는 순간 화면의 주를 표본으로 바꿔 그리고, 닫으면
  원래 주로 돌아온다. 안내가 열려 있는 동안 `save()`는 아무것도 하지 않는다.
- 설명은 가리키는 것의 오른쪽에 놓고 **요일 열 안에서 끝낸다.**
  좁은 화면에서 오른쪽에 자리가 없으면 그 아랫줄로 내린다 —
  note 칸으로 삐져나가면 거기 적힌 글자와 겹친다.
- **반투명 한 장을 덧댄 모양이다** (2026-08-20). 예시라는 것과 치울 수 있다는 것을
  한꺼번에 알리려면 "덮개"로 보여야 한다. 주간 표보다 사방 6px 크게 덮어 종이 밖으로
  걸치고, 테두리를 한 줄 두르고, **오른쪽 아래 모서리를 접어 둔다** — 잡아서
  치우면 된다는 표시. 필름은 잉크색 7.5%라 아래 글자가 그대로 읽힌다.
  설명 글의 배경은 `#eeeff2`, 곧 **필름이 덮인 종이색과 같은 값**이다.
  흰색으로 두면 필름에 구멍이 뚫린 것처럼 보인다 — 필름 농도를 바꾸면 이 값도 같이 바꾼다.
- 색은 쓰지 않는다. 설명은 보조색, 동사만 본문색. 손그림도 애니메이션도 없다.
  치워지는 것도 즉각적이다 — 걷히는 동작을 넣지 않은 것은 §7(취소선 말고는 움직이지 않는다)
  때문이다. 접힌 모서리가 그 역할을 대신한다.
- **아무 데나 누르면 이번만 닫히고**, `다시 보지 않기`를 누르면 영영 안 뜬다.
  거절은 한 번으로 끝나야 한다. 저장 키는 `clearweek:guide-seen`이며
  주 데이터가 아니므로 백업에 담기지 않는다.
- **거절한 뒤에도 되돌아갈 길을 둔다.** 제목을 꾹 눌러 여는 백업 줄에 `쓰는 법`이 있다.
  한 번 닫으면 영영 못 보는 안내는 안내가 아니다.
- 저장이 막혀 있으면(사생활 모드) 다시 보지 않기가 기억되지 않아 매번 뜬다.
  이건 알림(§5-2)이 이미 설명하는 상태다.
- 애니메이션 없음. 취소선 말고는 움직이는 것을 두지 않는다는 §7을 그대로 지킨다.

### 4-5. 소리 — 사각사각과 타자 (2026-08-20)

- **소리는 사람이 화면을 건드린 뒤에만 날 수 있다**(브라우저 규칙).
  긋기를 시작할 때 처음 깨우면 **그 첫 획이 통째로 조용하다.**
  그래서 아무 데나 처음 누르는 순간 미리 깨워 둔다(`Sound.unlock`). 한 번이면 된다.
  앱을 다시 열면 잠들어 있으므로 `visibilitychange`에서도 깨운다.
- **타자 소리**는 `input` 이벤트만 듣는다 — 글자가 실제로 바뀔 때 오는 유일한 신호다.
  `keydown`을 같이 들으면 한 글자에 두 번 울리고, 한글 조합 중에는 `keydown`이
  `Process`로만 와서 어차피 쓸 수 없다. 방향키처럼 글자가 안 바뀌는 키는 울지 않는다.
- 손잡이는 `SOUND.keyVolume` · `keyHz` · `keyQ` · `keyMs`.

### 4-3. 삭제

- 가로 방향은 긋기(빈 줄 위 드래그)와 지우기(그어진 줄 위 문대기)가 이미 쓰고 있어
  스와이프 삭제는 구분이 불가능. **롱프레스(0.5초) 삭제**로 이동.
- **꾹 누른 뒤 손을 떼면** 그 항목 하나가 지워진다.
- **꾹 누른 뒤 그대로 끌면** 손가락이 지나간 항목이 차례로 옅어지고, 떼는 순간
  전부 지워진다. 칸을 넘어가도 따라간다. 훑는 동안에는 칸 나누기가 끼어들지 않는다.
- 확인 다이얼로그 없음.

---

## 5. 데이터

로컬 저장만 사용합니다. `localStorage` (이 프로토타입은 본인 서버/Cloudflare Pages에
직접 배포하므로 사용에 제약 없음).

```js
// key: "clearweek:2026-W34"
{
  weekId: "2026-W34",        // ISO 주 식별자
  days: {
    mon: [ { id, text, struck, createdAt, strikes: [...] } ],
    tue: [...], wed: [...], thu: [...], fri: [...], sat: [...], sun: [...],
    free: [...]              // 8번째 자유 칸
  },
  notes:  { mon: "8/20 마감", ... }    // 요일 아래 작은 메모
  // 열 수는 저장하지 않는다 — 항목 수에서 매번 계산된다
}
```

- 주가 바뀌면 새 키를 생성. 이전 주 데이터는 그대로 보존(열람용).
- 자동 이월 없음. 마이그레이션 로직 없음.

### 5-1. 백업 — 초안의 "1단계 제외"를 뒤집는다 (2026-08-19)

초안은 내보내기/가져오기를 1단계에서 뺐다. 뒤집는 이유는 하나다.
**서버가 없으므로 사파리 데이터 하나가 유일한 사본이고, 그게 지워지면 끝이다.**
2주 실사용이 목적인 단계에서 2주치가 날아가면 검증 자체가 무산된다.
기능을 늘리는 게 아니라, 이미 있는 데이터가 사라지지 않게 막는 것이다.

- **설정 화면은 여전히 없다.** 상단 제목(`2026. 8.17-8.23`)을 0.5초 꾹 누르면
  머리말 아래에 한 줄이 열린다 — `내보내기` `가져오기`, 그리고 결과 한 마디.
  딴 데를 누르면 닫힌다. 평소에는 없는 것과 같다.
- **내보내기**: 저장된 모든 주를 파일 하나로 묶는다.
  `clear-week-2026-08-19.json` / `{ kind, version, savedAt, weeks: { "2026-W34": {…} } }`.
  아이폰은 다운로드가 어정쩡하므로 공유 시트(`navigator.share`의 파일 공유)를 먼저 쓰고,
  안 되면 `<a download>`로 떨어진다.
- **가져오기는 덮어쓰지 않는다.** 지금 없는 주만 채우고, 이미 있는 주는 손대지 않는다.
  결과를 `12개 주 가져옴 · 3개는 이미 있어 그대로 둠`처럼 그대로 말한다.
  되돌리기가 없는 앱에서 덮어쓰기는 유일한 사본을 지울 수 있다.
  기기를 바꾸거나 데이터가 지워진 뒤 되살리는 경우가 실제 용도이고, 그때는 대상이 비어 있다.
  확인 다이얼로그를 두지 않아도 되는 이유가 이것이다 — 지우는 동작이 아예 없다.
- 백업 파일이 아니거나 깨진 파일은 그 자리에서 거절하고 이유를 한 마디로 적는다.
- **한 주가 깨져 있어도 나머지는 살린다.** 내보낼 때 값이 깨진 주는 세어서
  `12개 주 내보냄 · 1개는 깨져서 빠짐`처럼 알린다 — 조용히 잘린 백업은 백업이 아니다.
  가져올 때도 주 모양이 아닌 값은 넣지 않는다. 넣으면 빈 주가 되는데,
  덮어쓰지 않는 규칙 때문에 나중에 제대로 된 백업을 가져와도 그 자리가 막힌다.
- 파일 이름의 날짜는 UTC가 아니라 이 자리의 날짜다 (한국에서 오전 9시 전에 내보내면
  UTC로는 어제라, 전날 저녁 파일과 이름이 겹친다).

### 5-2. 저장이 막히면 알린다 (2026-08-19)

사파리 사생활 모드나 용량 초과에서 `localStorage`는 예외를 던진다. 지금까지는
조용히 무시했고, 사용자는 다 적은 뒤에야 사라진 걸 알았다.

- 시작할 때 한 번 시험 삼아 써 보고, 저장 중 실패하면 그때도 잡는다.
- 머리말 아래 한 줄에 `저장 안 됨 — 사생활 모드이거나 용량이 찼습니다`가 뜬다.
  본문색 세미볼드 — 조용하되 눈에는 들어와야 한다.
  이 알림이 `지난 주 — 열람만`보다 먼저다.
- 화면은 그대로 동작한다. 적고 긋는 것을 막지 않는다. 남지 않는다는 것만 알린다.

---

## 6. 기술 제약

- **단일 HTML 파일** 또는 최소 파일 구성. 빌드 과정 없음.
- React CDN + Babel standalone, 또는 순수 JS. (기획자의 기존 작업 방식과 동일)
- 타깃: **iPhone Safari**. 홈 화면에 추가했을 때 앱처럼 뜨도록 PWA 메타 태그 포함.
  - `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`
  - `viewport-fit=cover` + safe-area-inset 대응
  - **확대 차단**: iOS 사파리는 `user-scalable=no`를 무시하므로,
    `touch-action: none`(문서 전체) + `gesturestart/change/end` preventDefault +
    두 손가락 `touchmove` 차단을 함께 건다. 한 손가락 제스처는 그대로 통과한다.
    입력창만 `touch-action: auto`로 열어 커서를 찍을 수 있게 둔다.
  - 512px 아이콘, `theme-color`
- 데스크톱에서도 열리되, 최적화 대상은 아이폰 세로.
- 외부 라이브러리 최소화. 애니메이션 라이브러리 불필요.
- 오프라인 동작 (서비스워커는 선택).

---

## 7. 디자인 방향

**참조 대상은 실제 종이 플래너입니다.** 스크린샷의 손글씨 주간 플래너 — 유성펜,
칸을 나눈 선, 삐뚤게 그어진 취소선.

- **색**: 배경은 흰색(`--paper` #ffffff), 구분선은 중성 회색(`--rule` #dedede).
  `color-scheme: light`로 고정해 다크모드에서도 색이 뒤집히지 않는다.
  잉크는 순검정이 아닌 짙은 남색 계열
  (실제 볼펜 잉크에 가까움). 강조색은 취소선 하나에만 허용.
- **타이포**: 손글씨체를 쓰지 말 것 (가독성 저하 + 유치해짐). 대신 약간의 인간미가 있는
  본문 산세리프. 요일 라벨만 다른 크기·자간으로 구분.
- **선**: 칸 구분선은 자로 그은 듯한 얇은 실선. 그림자·라운드·그라데이션 금지.
  칸을 나눠 생긴 세로선도 같은 실선 — 손그림으로 남지 않는다.
  표는 위아래만 닫는다 — 머리말 아래 선이 윗선, 마지막 칸 아래 선이 아랫선.
  좌우는 열어 둔다.
- **여백**: 요일 라벨이 왼쪽 여백을 이루고, 항목은 그 오른쪽에서 시작한다(`--label-w` 42px).
  한 주를 한 화면에 담아야 하므로 항목 높이는 `--item-height`(26px).
  스펙 초안의 44px보다 낮지만, 가로 드래그에는 이 정도 세로 여유로 충분하다.
  글자는 종이 손글씨에 가깝게 작게(`--item-font` / `--free-font` 모두 9.5pt).
- **애니메이션**: 취소선이 그어지는 것 하나에만 집중. 나머지 전환은 전부 즉각적으로.

이 앱에서 시각적으로 기억되어야 할 것은 **화면 가득 쌓인 취소선** 하나입니다.
그 외의 모든 요소는 조용해야 합니다.

---

## 8. 완료 기준

1. 아이폰 사파리에서 홈 화면에 추가 → 앱처럼 전체화면으로 열린다.
2. 8개 칸에 항목을 적고, 손가락으로 그어서 지울 수 있다.
3. 앱을 닫았다 열어도 데이터가 남아 있다.
4. 이전 주로 넘어가 지난 기록을 볼 수 있다.
5. 획 파라미터를 코드 상단에서 한 곳에서 조정할 수 있다.

---

## 9. 작업 순서 (1단계 내부)

1. ~~spec 4-2만 구현한 단일 페이지: 더미 항목 5개 + 드래그 긋기.~~ 완료
2. ~~아이폰 실기기 손맛 피드백 → 파라미터 튜닝.~~ 완료 (2회)
   - 획을 거의 직선으로, 두 줄은 줄마다 따로, 지우기는 지우개 방식, 사각사각 소리 추가
3. ~~7+1 주간 화면, 입력, localStorage, 주 이동.~~ 완료
4. **← 지금 여기.** 아이폰 홈 화면에 추가해 2주간 실사용 (spec 0의 검증 방법)

### 확정된 제스처

| 제스처 | 동작 |
|---|---|
| 빈 줄 위 가로 드래그 | 긋기 (지나간 만큼만, 줄 단위) |
| 그어진 줄 위 좌우 문대기 | 지우개 — 문지른 자리만 잉크가 벗겨짐 |
| 날짜 라벨 / `note` 탭 | 그 칸에 항목 추가 (＋ 버튼 없음) |
| 라벨을 꾹 눌러 아래로 끌기 | 요일 아래 메모 적기 (6.5pt 세미볼드) |
| 항목 탭 | 항목 편집 (그어진 항목은 불가) |
| 더블탭 | 남은 줄까지 자동으로 긋기 |
| 항목 롱프레스 0.5초 | 그 항목 삭제 |
| 롱프레스 후 그대로 드래그 | 지나간 항목 전부 삭제 |
| (칸 나누기는 제스처가 아니다 — 항목 수를 보고 저절로 나뉜다) | |
| 상단 제목 롱프레스 0.5초 | 백업 줄 열기 (내보내기 · 가져오기) |

### 저장 형태

획은 좌표 전체가 아니라 **시드 + 줄 안에서의 시작·끝 비율**로 저장한다.
같은 시드는 같은 획을 만들므로 용량이 작고, 화면 폭이 달라져도 비율이 따라간다.

```js
strikes: [ { line: 0, a: 0.01, b: 0.97, seed: 1074304443 } ]
```

부분적으로 지워진 상태는 저장하지 않는다. 완전히 지워진 획만 목록에서 사라진다.

### 지우개는 손을 뗄 때 결판을 낸다 (2026-08-20)

**양 끝은 문대는 손이 되돌아가는 자리라 한 번밖에 안 지나간다.** 그래서 가운데는
지워지는데 끝이 남았다 — "여러 번 지워도 흔적이 남는다"의 정체가 이것이다.
게다가 부분적으로 지워진 상태는 저장하지 않으므로, 화면과 저장된 것이 어긋난 채
계속 남아 있었다.

손을 떼는 순간 둘 중 하나로 정리한다.
- 구간의 **70% 이상**(`ERASE.finishRatio`)이 지워졌으면 **마저 지운다.**
- 아니면 **잉크를 온전히 되돌린다.** 저장된 것과 화면이 같아야 한다.

어중간한 상태는 남기지 않는다.

### 편집 중에 같은 글이 두 번 보이던 버그 (2026-08-19)

항목을 탭하면 원본을 `hidden`으로 숨기고 그 자리에 입력창을 놓는 구조인데,
`.item { display: flex }`가 브라우저 기본 `[hidden] { display: none }`을 이겨서
원본이 안 숨겨졌다. 같은 글이 둘로 보이다가 편집을 마치면 하나가 사라졌다.
저자 스타일은 특정도와 무관하게 기본 스타일을 이기므로, 숨김이 필요한 요소에는
`[hidden]` 규칙을 직접 적어 둔다 (`.item[hidden]`, `.backup[hidden]`).

---

## 10. 이 단계 이후

1단계가 통과하면(손맛이 종이에 근접):
- 아이패드 Swift Playgrounds에서 **위젯 익스텐션 생성이 가능한지** 확인 → 이것이
  네이티브 경로 결정의 분기점.
- 가능하면 Swift Playgrounds로 진행 (Mac 불필요, 개발자 계정 $99만).
- 불가능하면 클라우드 Mac 또는 중고 Mac mini 검토.

1단계가 통과하지 못하면(화면 위 취소선이 종이만 못함):
- 제품의 핵심 전제가 무너지므로, 개발자 계정 등록 전에 재검토.
- 대안 축: 긋기 대신 "한 주가 항상 보인다"(위젯)만으로 제품이 성립하는지 별도 검증.

---

## 11. 로그인과 동기화 — §2의 첫 줄을 뒤집는다 (2026-08-20)

### 왜 뒤집었나

§1의 "연동 없음, 계정 없음, 동기화 없음"은 **완성된 제품에 대한 약속**이지
개발 도중의 제약이 아니다. 그런데 §0이 요구하는 검증 방법은 "2주간 실사용"이고,
기획자는 폰과 PC를 오간다. **폰에 적은 것이 PC에 없으면 실사용 자체가 안 된다.**
검증을 못 하면 제품이 굴러가지 않으므로, 검증 도구로서 붙인다.

**이것은 최종 부품이 아닐 가능성이 크다.** 최종 형태인 iOS 네이티브에서는
iCloud(CloudKit)가 **계정도 로그인 화면도 없이** 기기 사이를 맞춰 준다.
그때 이 로그인은 통째로 사라져야 한다. 그러니 웹 쪽에 뿌리를 깊게 내리지 않는다 —
**로그아웃 상태의 앱은 예전과 완전히 같다.** localStorage 하나뿐이고,
서버가 죽어도 적고 긋는 데 아무 지장이 없다.

### 로그인 — 링크가 아니라 여섯 자리 코드

**코드는 기기가 아니라 메일 주소에 묶인다.** 어느 기기에서 받았든 아무 기기에나 넣으면 된다.
그래서 화면은 한 장이다 — 메일 주소 칸과 코드 칸이 **처음부터 같이 열려 있고**,
`코드 받기`는 코드가 없을 때만 누르는 버튼이다.
`코드 받기`를 먼저 누르게 강제했더니 "PC에서 받은 코드를 폰에 넣으면 될 텐데 왜 또 받지"
하는 혼란이 생겼다. 서버 로직이 아니라 화면 순서의 문제였다.
(코드는 한 번 쓰면 버려지므로, 기기마다 한 번씩은 받아야 한다.)


이메일 로그인을 붙이되 **매직 링크는 쓰지 않는다.** 이 앱은 홈 화면에 추가해서 쓰는데,
**홈 화면 앱과 사파리는 저장소를 따로 쓴다.** 메일에서 링크를 누르면 사파리가 열리고
세션이 사파리에 생긴다 — 홈 화면 앱은 여전히 로그아웃 상태다. 링크는 이 앱에서 못 쓴다.
그래서 메일로 **여섯 자리 코드**를 보내고 앱 안에서 입력받는다. 앱을 벗어나지 않는다.

- 로그인 자리는 **제목 롱프레스로 여는 서랍** 안이다. 설정 화면은 여전히 없다.
- 세션 쿠키는 `HttpOnly` · `Secure` · `SameSite=Lax`, 90일.
- 코드는 10분·5회, 요청은 1분에 3회까지. 코드는 해시로 저장하고 한 번 쓰면 버린다.
- **허용 목록(`ALLOWED_EMAILS`)이 없으면 서버가 열리지 않는다.** 없으면 아무 주소로나
  코드 메일을 쏠 수 있는 통로가 된다. 기획자 본인이 쓰는 물건이므로 닫아 두는 것이 기본값이다.

### 합치기 — 적은 것이 없어지지 않는 쪽으로 기운다

**판단은 서버 한 곳에만 둔다.** 클라이언트는 보내고 받은 것을 그대로 쓴다.
두 곳에 두면 규칙이 갈라지고, 갈라지면 무엇이 맞는지 아무도 모른다.

- 항목마다 `updatedAt`, 요일 메모마다 `noteAt`, 지운 항목은 `graves`에 표시를 남긴다.
  **지운 표시가 없으면 한쪽에서 지운 것이 다른 쪽에서 되살아난다.**
- 같은 항목은 나중에 고친 쪽을 따른다. 지운 표시보다 나중에 고쳤으면 고친 쪽이 이긴다 —
  **지워지는 것보다 남는 것이 낫다.**
- 순서는 `createdAt`으로 정한다. 배열 순서를 쓰면 기기마다 달라진다.
- 지운 표시는 60일 뒤 정리한다. 그보다 오래 꺼져 있던 기기에서는 지운 항목이
  되살아날 수 있다 — 그 편이 적은 것이 사라지는 것보다 낫다.
- 시각은 1e12를 넘으므로 `|0`을 쓰면 안 된다. 32비트를 넘어 값이 뒤집힌다.

### 처음 이을 때는 물어본다 (2026-08-20 추가)

합치기 규칙은 "적은 것이 없어지지 않는 쪽"이다. 같은 주를 폰과 PC에서 나눠 적었을 때는
이것이 맞다. 그런데 **기기를 처음 이을 때는 다르다** — 양쪽에 서로 다른 내용이 쌓여 있으면
전부 뒤섞이고, 사용자는 자기가 시키지 않은 합치기를 당한다. 실제로 그렇게 됐다.

- 로그인 직후 서버를 **보기만 한다**(`GET /api/sync/all` — 합치지도 쓰지도 않는다).
- **양쪽에 다 기록이 있을 때만** 묻는다: `합치기` / `이 기기 것으로` / `서버 것으로`.
  한쪽만 있으면 물을 것이 없다. 그냥 맞춘다.
- 묻는 동안에는 아직 아무것도 합치지 않는다. 고르는 순간에 일어난다.

### 전부 비우기 — 되돌릴 수 없으므로 두 번 누른다

`전부 비우기`는 서버와 **모든 기기**의 기록을 지운다. 한 번 누르면 `정말 비웁니다`로
바뀌고, 5초 안에 한 번 더 눌러야 실행된다. 모달은 두지 않았다.

되살아나는 것을 막는 장치가 핵심이다. **비운 시각(`reset:<email>`)을 서버에 남긴다.**
- 그보다 오래된 것을 아는 기기가 올리는 주는 **받지 않는다.** 받으면 방금 비운 것이
  그 기기에서 되살아난다.
- 그 기기는 응답에 실린 `resetAt`을 보고 자기 사본을 비운 뒤, 서버가 돌려준 것을 받는다.
- `이 기기 것으로`는 서버를 비우되 **이 기기 것은 남기고** 곧바로 올린다. 그것이 새 출발점이다.
- `서버 것으로`는 서버를 건드리지 않고 **내 것만 버린다.** 올릴 것이 없으면 서버 것만 내려온다.

### 맞추는 시점 — 진짜 실시간은 아니다

**WebSocket은 쓰지 않았다.** 지금은 이렇게 맞춘다.

- 고치고 0.9초 뒤 (연달아 적을 때 매번 보내지 않게)
- 화면을 다시 켤 때 — **기기를 바꾼 순간이 이때다**
- 화면이 켜져 있는 동안 15초마다
- 로그인 직후 한 번, 가진 주를 전부 올리고 전부 받아온다

한 사람이 두 기기를 쓰는 상황에서 두 화면을 동시에 보는 일은 없으므로,
**폰을 집어 드는 순간 맞춰지는 것과 실시간은 체감이 같다.** 진짜 실시간(WebSocket +
Durable Objects)은 서버 상태를 계속 들고 있어야 하고 요금제도 달라진다.
두 화면을 나란히 놓고 쓰는 일이 실제로 생기면 그때 올린다.

**밀기와 받기는 한 번에 한다** (`POST /api/sync`가 둘 다 한다). 나누면 그 사이에 갈라진다.

### 손이 닿아 있는 동안은 다시 그리지 않는다 (2026-08-20)

동기화가 받은 것을 반영하려고 `render()`를 돌리면 **그리던 획이 통째로 사라진다.**
고치고 0.9초 뒤 올린 응답이 손이 다시 닿은 순간에 도착하기 때문에,
"지운 직후에는 선이 잘 안 그어진다"로 나타났다. 실제로 획이 사라지고 있었다.

- 항목 위에 손이 닿아 있는 동안(`Gesture.busy`)에는 반영을 미룬다. 손을 떼면 맞춘다.
- **대개는 방금 내가 올린 것이 그대로 돌아온다.** 글과 획이 같으면 저장만 하고
  다시 그리지 않는다. 매번 그리면 화면이 깜빡이고 획이 지워진다.
  (시각 `updatedAt`이 달라도 보이는 것이 같으면 같은 것으로 친다.)

### 남아 있는 한계

- **KV에는 조건부 쓰기가 없다.** 두 기기가 정확히 같은 순간에 밀면 한쪽 변경이
  덮일 수 있다. 진 쪽은 자기 데이터를 여전히 들고 있으므로 다음 차례에 다시 올라가
  스스로 회복된다. 이걸 근본적으로 없애려면 Durable Objects가 필요하다.
- **적는 중에는 받지 않는다.** 입력창이 열려 있으면 화면을 갈아엎지 않고, 입력이
  끝나는 순간 맞춘다. 저장만 하고 화면을 그대로 두면 메모리의 주가 저장본과 어긋나
  다음 저장 때 방금 받은 것을 덮어쓴다.
- **오프라인에서 적은 것은 다음에 연결될 때 올라간다.** 그동안 화면은 그대로 동작한다.

---

## 12. 내 계정 — 설정 화면 금지를 뒤집는다 (2026-08-20)

### 푸터를 화면에 두려던 시도는 물렸다

만든 사람을 밝히는 줄(`만든사람 DADA` + 홈)을 표 바깥 아래에 두어 봤다.
표는 마지막 칸 아래 선으로 닫히고 푸터는 그 밖에 놓이므로 "페이지 각인"으로 읽힐 것이라
봤는데, **실제로 보니 화면을 해쳤다.** 한 주가 한 화면인 제품에서 아래 한 줄은 작지 않고,
§7이 말한 "화면에 남아야 할 것은 취소선 하나"와 정면으로 부딪힌다. 걷어냈다.

### 그래서 계정 화면을 둔다

§2는 설정 화면을 금지했다. 그때는 설정할 것이 없었다. **로그인이 생기면서 계정이
생겼고, 계정에는 둘 자리가 필요하다.** 조항이 틀린 게 아니라 상황이 달라졌다.

- **머리말 오른쪽 끝의 사람 표시로 연다** (2026-08-20 추가). 처음에는 제목 롱프레스만
  두었는데, 서랍이던 시절에는 숨겨도 됐지만 **계정 화면이 생긴 이상 들어가는 문은
  보여야 한다.** 로그인해 두고 어디서 푸는지 못 찾으면 그건 문이 아니다.
  17px 선 그림, 화살표보다 옅게 — 주 이동이 여전히 머리말의 주인이다.
  **로그인돼 있으면 진해진다**: 열어 보지 않아도 맞춰지는 중인지 알 수 있다.
- 제목 롱프레스도 그대로 둔다 — 숨은 지름길로 남는다.
- 담는 것: **동기화 상태 · 계정(로그인/로그아웃) · 백업 · 쓰는 법 다시 보기 ·
  만든사람 DADA**. 앱의 뒷면 전부가 여기 한 곳에 있다.
- **주간 화면에는 아무것도 붙이지 않는다.** 머리말과 표뿐이다.
  덮는 방식이라 표의 높이도 줄지 않는다 — 서랍 줄이 한 줄을 가져가던 것보다 낫다.
- 서버가 없으면 `계정` 줄 자체가 나타나지 않는다. 라벨만 남은 빈 줄을 두지 않는다.
- 만든 사람은 맨 아래 가운데, 6.5pt 보조색. 홈은 19px 네모 하나, 새 창으로 연다.

계정 화면은 머리말까지 덮으므로 여는 동안 사람 표시는 가려진다. 닫는 문은 `닫기` 하나다.

**이것이 "설정 화면"의 상한선이다.** 켜고 끄는 스위치를 늘리는 자리가 아니다.
여기 들어갈 수 있는 것은 계정, 데이터를 꺼내고 넣는 일, 그리고 이 앱이 무엇인지뿐이다.

## 13. 로그인 없이 쓰는 것이 기본이다 (2026-08-20)

§11에 이미 적었지만 다시 못박는다. **서버가 없어도 앱은 완전히 동작한다.**

- `/api/me`가 404거나 KV가 안 묶여 있으면 `Sync.ready`가 거짓이 되고,
  **서랍에 로그인 버튼이 아예 나타나지 않는다.** 눌러도 안 되는 것을 내놓지 않는다.
- 그 상태에서 서랍은 `이 기기에만 저장됩니다`라고만 말한다.
- 서버를 켜면(`SETUP.md`) 버튼이 저절로 나타난다. 앱을 고칠 필요가 없다.

### 메일 발송 — 왜 Resend인가 (2026-08-20 조사)

MailChannels의 Cloudflare Workers 무료 발송은 **2024년 8월 31일에 끝났다.**
지금 Cloudflare 공식 문서가 안내하는 경로가 Resend다(월 3,000통 무료).

**도메인이 없어도 된다.** 발신 주소로 `onboarding@resend.dev`를 쓰면
**가입할 때 쓴 본인 주소로만** 메일이 간다 — 혼자 쓰는 이 앱에는 이 제한이 정확히 맞는다.
남에게 열려면 그때 도메인을 인증하면 된다.
