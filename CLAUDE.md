# Clear Week

종이 주간 플래너를 그대로 옮긴 웹 앱. 빌드 없는 단일 HTML 파일.
**작업 전 `clearweek-spec.md`를 반드시 읽을 것.** 그 문서가 판단 기준이다.

배포: Cloudflare Pages → https://clear-week.pages.dev (브랜치 푸시하면 자동 갱신)

---

## 이 제품의 규칙 — 판단이 필요할 때 여기로

1. **취소선은 삭제가 아니라 축적이다.** 그은 항목은 그 자리에 남는다.
   흐려지지도, 접히지도, 아래로 내려가지도 않는다.
   완료 항목을 숨기거나 정렬로 내리는 기능은 **영구히 만들지 않는다.**
2. **자동 이월 없음.** 못 한 일은 다음 주로 넘어가지 않는다.
3. **알림·태그·통계·다크모드 없음.** (spec §2 목록 엄수)
   여기서 '통계'는 **사용자에게 보여주는** 달성률·연속기록·점수다.
   만든 사람이 터미널에서 보는 행동로그는 다른 것이다 (spec §14) —
   **앱 화면에 숫자가 나타나는 순간 그건 §2 위반이다.**
   뒤집은 것 둘 — **첫 실행 안내**(spec §4-4)와 **로그인·동기화**(spec §11).
   로그인은 검증 도구다. 최종 형태인 네이티브에서는 iCloud가 계정 없이 맞춰 주므로
   **통째로 사라져야 한다.** 그래서 뿌리를 깊게 내리지 않았다 —
   **로그아웃 상태의 앱은 예전과 완전히 같다.**
4. **애매하면 넣지 말고 물어본다.** 경쟁력은 기능 수가 아니라 덜어냄이다.
5. **손그림으로 남는 것은 취소선 하나뿐.** 칸을 나누는 세로선은 없앴다 —
   열은 여백으로만 갈린다 (spec §3).
6. **손이 덜 가는 쪽이 옳다.** 적은 것을 보면 알 수 있는 일은 사람에게 시키지 않는다.
   칸 나누기가 자동이 된 이유다.

---

## 파일

| 파일 | 내용 |
|---|---|
| `index.html` | 전부. CSS·JS 인라인, 외부 의존 0, 빌드 없음 |
| `clearweek-spec.md` | 기획서. 결정과 이유가 여기 쌓인다 |
| `tests/smoke.mjs` | 회귀 테스트 (`node tests/smoke.mjs`) |
| `manifest.webmanifest`, `icon-180.png`, `icon-512.png` | PWA |
| `functions/_lib.js` | 서버 공통 — **합치기 규칙이 여기에만 있다** |
| `functions/api/…` | 동기화·비우기 (Cloudflare Pages Functions). 로그인은 Firebase가 맡는다 |
| `tests/merge.mjs` | 합치기 검사 (`node tests/merge.mjs`) — 브라우저 불필요 |
| `functions/_log.js` | 행동로그 문지기 — **무엇을 담을 수 있는지가 여기에만 있다** |
| `tools/stats.mjs` | 로그 읽기 (터미널). **앱 안에는 숫자를 두지 않는다** |
| `tests/log.mjs` | 문지기 검사 (`node tests/log.mjs`) — 브라우저 불필요 |
| `SETUP.md` | 서버를 켜려면 대시보드에서 해야 하는 일 |
| `wrangler.example.toml` | 로컬 개발용 견본. **`wrangler.toml`로 커밋하면 대시보드가 잠긴다** |

`index.html` 안의 구조 — 위에서부터 CSS 변수 → `STROKE`/`ERASE`/`SOUND`/`SPLIT`
상수 → 유틸 → 저장·백업 → 소리 → `StrikeItem`(항목 하나) → `App`(화면).
칸 나누기는 `columnCount()`/`applyColumns()` 함수 둘뿐이다.

---

## 확정된 제스처

| 제스처 | 동작 |
|---|---|
| 요일 칸의 **빈 곳** · **글자 옆 빈 자리** 탭 | 그 칸에 항목 추가 (＋ 버튼 없음) |
| **펜을 빈 곳에 대고 그냥 쓰기** | 깔려 있던 판이 깨어나 크게 적힌다 — **탭 없음** (spec §4-7) |
| `note` 칸의 **빈 곳** 탭 | note에 항목 추가 |
| **왼쪽 여백**(날짜와 그 아래) 탭 | 요일 아래 메모 (6.5pt 세미볼드) |
| **요일 아래 메모** 위에서도 항목과 똑같이 | 긋기 · 지우개 · 더블탭 · 탭 편집 · 롱프레스 삭제 (spec §4-6) |
| 항목 탭 | 편집 (그어진 항목은 불가) |
| 주 이동 | **날짜 옆** 화살표. **지난 주도 편집 가능** (spec §3) |
| `today` | 이번 주를 벗어났을 때만 화살표 오른쪽에 나타남 |
| 항목 위 가로 드래그 | 긋기 — 지나간 만큼만, 줄 단위 |
| **왼쪽 여백에서 시작해 가로로** | 그 높이의 항목을 긋는다 (spec §4-2) |
| 그어진 줄 위 좌우 문대기 | 지우개 — 문지른 자리만 잉크가 벗겨짐 |
| 넓게 문대기 | 지나간 줄이 전부 지워진다 — 칸·단을 넘어가도 (spec §4-2) |
| 항목 더블탭 | 남은 줄까지 자동으로 긋기 |
| 항목 롱프레스 0.5초 | 그 항목 삭제 |
| 롱프레스 후 그대로 드래그 | 지나간 항목 전부 삭제 |
| **아무 데서나 아래로 당기기** | 새로고침 (spec §16). 세로 우세 + 항목 위 아닐 때만 |
| 머리말 오른쪽 **스피커 표시** 탭 | 소리 켜고 끄기 — 바로 바뀐다 (spec §4-5) |
| 머리말 오른쪽 **사람 표시** 탭 | **내 계정** 열기 (동기화 · 로그인 · 백업 · 쓰는 법 · 만든사람) |
| 상단 제목 롱프레스 0.5초 | 같은 곳 — 숨은 지름길 |

**빈 곳을 누르면 그 자리에 적힌다** (spec §4-2). 왼쪽 여백이면 요일 아래 메모,
요일 칸이면 항목, note면 note. **날짜를 눌러 만들기**와 **꾹 눌러 아래로 끌어
메모 열기**는 없앴다 — 누른 자리와 적히는 자리가 어긋나 있었다.

**첫 실행에 안내 카드 한 장이 뜬다** (spec §4-4). 가운데 카드, 줄 넷,
줄마다 표시 하나와 한 문장 — ＋생성 · ↔완료 · ✕삭제 · 사람 기기간 연동.
**안내에서만 색을 쓴다** (주황·초록·빨강·파랑). 주간 표는 여전히 단색이다.
서버가 없으면 (spec §13) 로그인 줄이 아예 빠진다.

**한 번에 한 줄씩 짚는다.** 한 줄만 진하고 나머지는 옅다. 아무 데나 누르면
다음 줄로 내려가고, **끝 줄에 닿아야 닫는 손잡이가 살아난다** — 그전에는
카드 밖을 눌러도 다음 줄로 갈 뿐이다.

**안내는 앱의 상태를 건드리지 않는다.** 표본 주를 그리고 셀로판지를 덧대던
방식은 물렸다 — 자리 다툼이 끝나지 않았고, 표본을 끼우느라 `save()`·
`currentWeek()`·`adopt()`에 "지금은 표본이니 멈춰라" 가드를 심어야 했다.
**안내 하나가 저장·동기화 경로에 손을 뻗고 있었다.** 지금은 그냥 뜬 카드다.

`다시 보지 않기`는 **체크상자**다 — 체크하고 닫아야 영영 안 뜬다. 그냥 닫으면
다음에 또 뜬다. 카드 밖을 눌러도 닫히고, 어느 길로 닫든 체크한 뜻은 지킨다.
`내 계정 → 쓰는 법`으로 다시 열 수 있다.

**요일 아래 메모도 긋는다** (spec §4-6). 항목과 같은 `StrikeItem`을 쓰되 옷만
`.sublabel`이다 — **메모는 항목이 아니라서** `.item`으로 세는 두 곳(훑어 지우기,
칸 넘침 `+N`)에 끼어들면 안 된다. 그은 메모는 글자 위든 여백이든 눌러도 안 열리고,
꾹 누르면 글자와 획이 함께 사라진다. 획은 `noteStrikes`, 무덤(`graves`)은 없다.

**펜으로는 그냥 쓴다** (spec §4-7). 손글씨 변환은 입력창 위에서만 도는데, 9.5pt
줄에서는 팔이 안 펴진다. **칸마다 투명한 입력창을 미리 깔아 둔다** — 탭이 없다.
깨어나는 길은 **포커스 하나**다: 스크리블은 포인터 없이 포커스만 주므로
`pointerType`으로 가르면 안 된다. **손가락은 포커스가 온 시각으로 거른다**
(`PAD.guardMs`). 잠든 판은 `inputmode="none"`이라 자판을 안 부른다.
**판은 적힌 것들 아래에서 시작한다** — z-index로는 못 막는다. 스크리블은 포인터
히트 테스트를 안 따라서, 판이 글자 밑에 깔려 있기만 해도 iOS가 그 위의 펜을
낚아채 **취소선이 새 글자로 들어갔다.** 글자가 있는 높이에는 판을 두지 않는
것이 유일한 방법이다 (`placePad`). 자리가 `PAD.minRoom`보다 좁으면 판을 숨긴다.
**깨어나도 상자는 안 그린다** — 테두리·그림자를 두니 종이 위에 창이 떴다.
**`.cell-body`는 `pointer-events: none`이다** — 칸을 덮는 상자라 안 그러면
빈 곳까지 가로채 펜이 판에 닿지 않는다. **깨어 있으면 칸의 클릭은 물러난다** —
작은 줄이 열리면 포커스를 뺏겨 판이 곧바로 잠든다.

**칸 나누기는 제스처가 아니다.** 한 열에 안 들어가면 저절로 나뉜다 — 개수가 아니라
실제로 넘치는지를 본다(화면 높이에 따라 한 열에 3줄이냐 4줄이냐가 달라진다).
요일 칸만, 최대 3단. 세로선은 그리지 않는다.

**여백에서 그어도 인식한다** (spec §4-2). 사람들은 글자 왼쪽, 날짜가 있는 여백에서
손을 긋기 시작한다 — 실제 사용에서 그랬다. **가로로 움직인 것이 분명해지는 순간**
그 높이의 항목에게 손짓을 넘긴다(`App.itemAtY` → `StrikeItem.onDown(e, from)`).
그전에는 넘기지 않는다 — 여백의 롱프레스는 항목 삭제가 아니다. 넘겨받은 손짓에는
**롱프레스를 걸지 않는다.** 나뉜 칸에서는 왼쪽 단부터.
**잉크는 글자에서부터 나온다** — 그려지는 것도 저장되는 것도 줄 양끝
`STROKE.overshoot`까지로 자른다. 안 자르면 날짜 위로 줄이 지나간다.

**지우개는 손가락 아래에 있는 것을 지운다** (spec §4-2). 예전에는 손짓을 붙든
항목에만 갇혀 있어서 넓게 문대도 한 줄씩만 지워졌다. 이제 매번 그 자리의 항목을
찾아 문댄다(`elementFromPoint` → `.item`, 훑어 지우기와 같은 방식) — 칸도 단도
넘어간다. 자국(mask)은 항목마다 따로이고, **손을 뗄 때의 정리도 닿았던 항목
전부에 걸린다.** 어느 줄인지는 여전히 가장 가까운 한 줄이다 — 넓게 지우려면
넓게 문대면 된다. 메모는 `.item`이 아니라 이 길로는 안 닿는다.

**제스처 공간이 겹치지 않게 설계돼 있다.** 새 제스처를 추가할 땐
(가로/세로) × (그어짐/안 그어짐) × (항목/빈 공간/라벨) 조합을 먼저 확인할 것.
**칸 안 세로 드래그는 지금 비어 있다** — 페이지 스크롤도 칸 나누기도 쓰지 않는다.

---

## 튜닝 손잡이 — 전부 파일 상단에 모여 있다

- CSS 변수: `--item-font`(9.5pt) `--free-font` `--sublabel-font`(6.5pt)
  `--item-height`(26px) `--label-w`(38px) `--free-width` `--rule`(#dedede)
  `--split-scale-1`(0.84) `--split-scale-2`(0.72)
- `STROKE` — 획 흔들림·굵기·확정 임계값·롱프레스 시간
- `ERASE` — 지우개 반경·**줄을 고르는 세로 도달거리(`reachPx`)**·세기·마무리 비율
- `SOUND` — 사각사각 + 타자 소리. **두 음량은 같은 눈금이 아니다** — 톡은 한 순간이라
  적힌 값보다 훨씬 작게 들린다. 그래서 `keyVolume`이 1을 넘는다 (spec §4-5).
  `ignoreSilent`는 **기기 무음을 무시할지** — **기본 켜짐.** 대신 머리말 스피커로
  끌 수 있다. **무시할 권리는 끌 자리를 준 다음에 생긴다** (spec §4-5)
- `AUTO_SPLIT` — 최대 몇 단까지 나눌지 (`maxColumns` 3)
- `PAD` — 깨어났을 때 글자 크기(`fontPx`)·손가락 포커스를 거르는 시간(`guardMs`)·
  판을 둘 최소 빈자리(`minRoom`)
- `PULL` — 당겨서 새로고침 (`distance` 72px·`slopPx`)
- `LOG` — 행동로그. **지금은 `enabled: false`** (spec §14). `respectDNT`는 추적 거부 존중
- `CAL` — 캘린더 연결 (spec §15). **`apiKey`·`projectId`가 비면 기능이 안 보인다**

---

## 저장

`localStorage`, 주별 키 `clearweek:2026-W34`. 안내 거절은 `clearweek:guide-seen`,
소리 끔은 `clearweek:sound`.
저장이 막히면(사생활 모드·용량 초과) 머리말 아래에 알린다 — 조용히 실패하지 않는다.
백업은 모든 주를 파일 하나로 묶는다(`clear-week-YYYY-MM-DD.json`).
**가져오기는 덮어쓰지 않는다** — 지금 없는 주만 채운다 (spec §5-1).

**로그인은 캘린더와 같은 계정이다** (spec §11, 2026-08-24). Firebase Auth 하나로
Clear Week 서버와 캘린더가 함께 이어진다 — 별도 "캘린더 연결" 화면 없음.
이메일+비밀번호, 없으면 자동으로 만든다. `refresh`만 `localStorage`에 저장,
짧은 `idToken`은 메모리에만.

Clear Week 서버 요청은 매번 `Authorization: Bearer <idToken>`을 붙인다.
서버는 Google의 JWK로 서명·발행자·수신자·만료를 검증해 email을 뽑는다
(`firebaseEmail()`). 세션 쿠키·자체 인증(코드 메일·`ALLOWED_EMAILS`·Resend·KV
세션)은 통째로 걷어냈다.

**합치기 규칙은 `functions/_lib.js` 한 곳에만 있다** — 클라이언트는 판단하지 않는다.
항목마다 `updatedAt`, 지운 항목은 `graves`에 표시가 남는다(없으면 되살아난다).
**기기를 처음 이을 때 양쪽에 다 기록이 있으면 물어본다** — 합치기/이 기기/서버 (spec §11).
`전부 비우기`는 비운 시각을 서버에 남겨, 그보다 오래된 기기가 올리는 것을 막는다.
**로그아웃 상태에서는 서버를 전혀 건드리지 않는다.**
**올리는 중에 고친 것은 응답에 덮이지 않는다** — `App.rev`가 오르면 그 응답은
낡은 것이라 버리고 다시 올린다. 연달아 그을 때 두 번째부터 사라지던 원인이었다.
Firebase 설정(`CAL.apiKey`·`projectId`)이 비어 있으면 **로그인 버튼이 아예
나타나지 않는다** (spec §13).

머리말의 사람 표시는 **로그인돼 있으면 진해진다** — 열어 보지 않아도 상태를 안다.

**방문자 행동로그는 붙어 있지만 꺼져 있다** (`LOG.enabled = false`, spec §14).
읽으려면 터미널이 필요한데 그럴 형편이 아니라서다 — **읽지 못하는 기록은
남의 기기에서 도는 짐이다.** 방문 수준은 Cloudflare Web Analytics가 대시보드로
보여 준다. 지운 게 아니라 끈 것이고, 검사는 켜 놓고 잰 뒤 다시 끈다.
켜려면 그 한 줄을 `true`로. **적은 글자는 절대 나가지 않는다:** 담을 수 있는
것을 `functions/_log.js`가 화이트리스트로 못박는다. 데이터셋을 안 묶으면
서버가 `501`을 주고 앱이 한 번 두드려 보고 스스로 그만둔다.

**캘린더(Dada Calendar)에서 받아만 온다** (spec §15). 저쪽은 Firebase지만 SDK를
넣지 않는다 — REST에 그냥 `fetch`한다(CORS 열려 있음, 중계 서버 불필요).
**한 방향이다** — 이쪽에서 적은 것도 지운 것도 저쪽으로 안 간다.
**저쪽에 쓰는 코드가 아예 없다**: 읽기(`runQuery`) 하나뿐이다. 일정의 원본은
캘린더에 있고, 주간표는 그것을 종이 위에 펼쳐 보는 자리다 — 종이에 그은 것이
원본을 바꾸면 안 된다.
받아 오는 것은 `kind === 'task'` 중 **안 끝난 것**뿐이다. 저쪽 id에서 이쪽 id를
유도해(`dc-<origId>`) 몇 번을 받아 와도 중복이 안 생긴다.
저쪽에서 사라진 것(fetch 응답에 없음)은 이쪽에서도 지우고 무덤에 남긴다.
이쪽에서 지운 것은 `graves`가 막아 다시 안 온다 — 저쪽 문서는 그대로다.
**받아 오는 때는 주 이동과 로그인 직후뿐이다** — 적을 때마다 묻지 않는다.
로그인은 Clear Week 것과 **같은 계정**이다 (spec §11).

**주간 화면에는 머리말과 표뿐이다.** 만든 사람도 백업도 로그인도 전부
`내 계정` 화면 안에 있다 (spec §12). 푸터를 화면 아래에 두려던 시도는 물렸다 —
한 주가 한 화면인 제품에서 아래 한 줄은 작지 않았다.
획은 좌표가 아니라 **시드 + 줄 안에서의 시작·끝 비율**로 저장한다
(`strikes: [{ line, a, b, seed }]`). 용량이 작고 화면 폭이 바뀌어도 따라간다.
부분적으로 지워진 상태는 저장하지 않는다.

```js
{ weekId, days: { mon: [{id,text,struck,createdAt,strikes}], …, free: [] },
  notes: { mon: "8/20 마감" }, noteAt: { mon: 1755… },
  noteStrikes: { mon: [{line,a,b,seed}] } }
```
**메모의 글자와 획은 한 몸이다** — `noteAt` 하나로 이긴 쪽을 통째로 가져온다.
따로 합치면 이쪽 글자에 저쪽 획이 얹힌다 (spec §4-6).
열 수는 저장하지 않는다 — 항목 수에서 매번 계산된다.

---

## 작업 방식

- 작업은 세션 브랜치(`claude/…`)에서 하고, 끝나면 **`claude/clear-week-planner-y2glcy`에
  병합해서 푸시한다.** 그 브랜치가 저장소 기본 브랜치이자 Cloudflare Pages 배포 대상이다.
  (`main`은 없다 — 만들려면 Pages 대시보드에서 프로덕션 브랜치도 같이 바꿔야 한다.)
- **변경하면 `clearweek-spec.md`도 같이 갱신한다.** 결정의 이유가 남아야 한다.
- 푸시 전에 `node tests/smoke.mjs`와 `node tests/merge.mjs`를 둘 다 돌린다.
  새 동작을 넣으면 검사도 추가한다.
- 스크린샷으로 눈으로 확인한다 (`page.screenshot`). 수치만으로는 레이아웃을 못 본다.

### 검증할 수 없는 것 — 실기기 아이폰에서만 확인 가능

햅틱 진동, 키보드가 올라올 때 화면 밀어올림, 확대 차단, 실제 손맛.
판이 깨어나는 것까지는 검사한다(포커스만으로, 펜 포인터로 둘 다).
**iOS·iPadOS는 무음이면 Web Audio를 통째로 막는다** — 그래서 기기 무음은 무시하고
머리말 스피커로 끄게 했다 (spec §4-5).
**"확인했습니다"라고 말하지 말 것** — 사람이 폰에서 확인해 준 것만 아래로 옮긴다.

**확인된 것** (사람이 실기기에서 봐 준 것만 여기 온다)
- 사각사각과 타자 톡의 **음량 균형** (2026-08-21) — 파형으로 맞춘 −4.5 dB가
  실제 귀에서도 맞았다 (spec §4-5). 무음 스위치를 끄면 소리는 난다.
- **연달아 긋기**가 배포본에서 남는 것 (2026-08-21, spec §11)
- **아이패드에서 소리가 나는 것** (2026-08-21) — 사파리의 `interrupted`를
  깨우지 않던 것을 고친 뒤 (spec §4-5)
- **애플펜슬 손글씨 변환(스크리블)이 실제로 도는 것** (2026-08-24) —
  빈 곳에 펜을 대고 그냥 쓰면 **탭 없이** 판이 깨어나 글자로 들어간다.
  깔아 둔 입력창 방식이 실기기에서 통한다는 뜻이다 (spec §4-7)

---

## 지금 상태 (1단계 완료)

한 주가 한 화면에 고정되고, 적기·긋기·지우기·주 이동·저장·백업이 동작한다.
칸은 넘칠 때만 스스로 나뉜다. 지난 주도 고쳐 쓸 수 있다.
로그인 하나로 폰·PC·캘린더가 함께 맞춰진다 (Firebase Auth, 2026-08-24).
방문자 행동로그는 붙어 있으나 꺼 두었다.
요일 아래 메모에도 줄을 그을 수 있다.
여백에서 시작한 긋기도 인식한다.
넓게 문대면 지나간 줄이 한 번에 지워진다.
캘린더(Dada Calendar)에서 이번 주 할 일을 받아 온다 (한 방향).
펜을 빈 곳에 대면 탭 없이 바로 크게 쓴다 — **실기기에서 확인됨.**
아무 데서나 아래로 당기면 새로 받는다.
회귀 테스트 307 + 합치기 21 + 행동로그 49개 통과.

**실기기에서 확인된 것 셋**: 연달아 긋기 고침·소리 균형 (2026-08-21),
애플펜슬 손글씨 변환 (2026-08-24).
**Cloudflare Web Analytics를 켰다** — 방문 수준은 그쪽 대시보드에서 본다.
Analytics Engine 바인딩은 지웠다. 로그가 꺼져 있으므로 필요 없고,
켜지 않은 채 걸어 두면 배포가 막힌다 (아래 10번).

### 알려진 미해결

1. **되돌리기 없음.** 롱프레스 삭제는 확인도 취소도 없다.
2. **항목 높이 26px는 검증되지 않았다.** 기획서 초안은 "긋기를 위해 44px 양보
   불가"였는데, 한 화면 고정 요구에 밀려 내려갔다. 손맛에 영향이 있는지 미확인.
3. **백업 경로가 실기기에서 미검증.** 아이폰 공유 시트로 파일이 제대로 떨어지는지,
   홈 화면 앱(standalone)에서도 되는지는 실기기에서만 알 수 있다.
4. **적은 것을 지키는 장치는 `blur` 말고도 둘 더 있다** — 딴 데를 누르는 순간과
   화면을 떠나는 순간 (spec §4-1). 모바일 자판의 blur는 믿을 수 없다.
5. **탭으로 여는 입력창은 반드시 `click`에서 연다.** `pointerup`에서 열면 터치
   브라우저가 click 시점에 주는 포커스에 빼앗겨 곧바로 닫힌다 (spec §4-1).
   **안드로이드 검사는 `hasTouch` 컨텍스트로 돌린다** — 마우스로는 안 드러난다.
6. **소리를 끄면 `resume()`도 막아야 한다.** 안 그러면 끈 뒤 아무 데나 한 번
   누르는 순간 `suspend()`가 풀리고, 소리는 안 나도 **재생 갈래를 붙든 채라
   남의 앱 소리를 계속 가로챈다.** 끌 때 갈래도 `auto`로 놓아 준다 (spec §4-5).
7. **`overflow: hidden`인 상자도 굴러간다.** 포커스를 주면 브라우저가 그 요소를
   보이려고 조상 상자를 굴려, 칸에 적힌 것들이 밀려 사라졌다 (spec §4-1).
   칸 안에서 입력창을 열 때는 `App.focusInCell`을 쓴다 —
   `focus({preventScroll:true})` + 열 다시 맞추기. 직접 `focus()`하지 말 것.
8. **`[hidden]`은 전역 규칙으로 막아 뒀다.** 저자 스타일 `display`가 브라우저 기본
   `[hidden]`을 이겨서 세 번 밟았다(`.item`·`.backup`·`.account .row`).
   지금은 `[hidden] { display: none !important; }` 한 줄이 원천 차단한다.
9. **`wrangler.toml`을 저장소에 두면 안 된다.** 있으면 Pages가 설정을 그 파일에서만
   읽고 대시보드 Bindings 편집을 잠근다. 견본은 `wrangler.example.toml`이고
   `.gitignore`가 `wrangler.toml`을 막는다 (2026-08-20에 한 번 밟았다).
10. **서버는 배포·실기기 미검증.** 모의 서버로만 확인했다. KV 바인딩·메일 발송은
   `SETUP.md`대로 대시보드에서 켜야 동작한다.
11. **동시에 밀면 한쪽이 덮일 수 있다.** KV에 조건부 쓰기가 없어서다.
   다음 차례에 스스로 회복되지만, 근본 해결은 Durable Objects (spec §11).
   **`syncAll`(로그인·기기 잇기)에는 `rev` 방패가 없다** — 그 길은 덮어쓰는 것이
   목적이라서다. 창이 좁다(서랍이 열려 있는 동안엔 못 긋는다). 이유는 spec §11.
12. **Analytics Engine은 계정에서 먼저 켜야 한다.** 안 켜고 바인딩만 걸면
    **배포가 통째로 실패하고 사이트 갱신이 멈춘다**(`You need to enable
    Analytics Engine`). 로그가 선택이라고 해서 안전한 게 아니다 —
    바인딩 하나가 배포를 볼모로 잡는다. 켜는 것이 먼저, 바인딩이 나중
    (SETUP.md 5-2). 2026-08-21에 한 번 밟았다.
13. **행동로그도 실기기 미검증.** 대시보드에서 Analytics Engine 데이터셋을
    묶어야 첫 이벤트가 들어간다 (SETUP.md 5-3). 화면을 덮는 순간의 `sendBeacon`이
    아이폰 홈 화면 앱에서 정말 나가는지는 실기기에서만 알 수 있다.
14. **로그인 통일(Firebase)은 실제 프로젝트로 미검증.** 모의 REST로만 확인했다.
    2026-08-24에 자체 인증을 걷어내고 Firebase Auth로 통일해 배포했으니
    (spec §11), 사람이 실기기에서 로그인·기기 잇기·캘린더 왕복을 봐 줘야 한다.
    Firebase 콘솔 → Authentication → Settings → 승인된 도메인에
    `clear-week.pages.dev`가 있어야 한다 (SETUP.md 2번).
15. **로그에 새 자리를 늘리려면 세 곳을 같이 고친다** — `functions/_log.js`의
    화이트리스트, `tests/smoke.mjs`의 허용 키 목록, `tools/stats.mjs`의 blob 번호.
    하나만 고치면 조용히 버려지거나 엉뚱한 칸을 읽는다.
16. **홈 화면 앱 상단 바는 `default`가 최선이고, 그 이상은 손댈 수 없다** (spec §16).
    **iOS는 `theme-color`를 안 본다.** 고를 수 있는 것은
    `apple-mobile-web-app-status-bar-style`의 셋뿐이고, 흰 바탕+검은 글자는
    `default` 하나뿐이다. CSS로 그 바를 칠할 방법은 없다.
    **이 값은 홈 화면에 추가하는 순간 iOS가 붙들어 둔다** — 고친 뒤에는 앱을
    지우고 다시 추가해야 반영된다. 사용자 기기에서 남색(`#1d2b50`)으로 뜬 이유는
    끝내 설명하지 못했다 (우리 CSS·아이콘·git 이력 어디에도 그 값이 배경으로 없다).

### 해소됨 (2026-08-19)

- ~~칸이 꽉 차면 넘친 항목이 조용히 사라진다~~ → 칸 오른쪽 아래에 `+3` 표시.
  숨기거나 접지 않고 잘렸다는 사실만 알린다 (spec §3).
- ~~백업·내보내기 없음~~ → 제목 롱프레스로 여는 백업 줄. 가져오기는 덮어쓰지 않는다 (spec §5-1).
- ~~`localStorage` 실패가 조용하다~~ → 머리말 아래 알림 (spec §5-2).
- ~~편집할 때 같은 글이 두 번 보인다~~ → `.item[hidden]` 규칙 추가.
  저자 스타일 `display: flex`가 브라우저 기본 `[hidden]`을 이기고 있었다.
- ~~칸을 나누려면 매번 세로선을 그어야 한다~~ → 항목 수를 보고 저절로 나뉜다 (spec §3).

### 다음 갈림길 (spec §10)

아이패드 Swift Playgrounds에서 **위젯 익스텐션 생성이 가능한지** 확인.
가능하면 네이티브 진행(Mac 불필요), 불가능하면 클라우드 Mac 또는 중고 Mac mini.
위젯이 실질적인 제품이다 — "한 주가 항상 펼쳐져 있음"이 이 제품의 경쟁력이고,
손맛은 계속 쓰게 만드는 장치다.
